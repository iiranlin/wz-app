<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:windowBackground">@color/colorWhite</item>
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
    </style>

    <!--页面中部loading加载框-->
    <style name="loading_progress_dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:background">@null</item>
        <item name="android:windowBackground">@null</item>
        <item name="android:backgroundDimEnabled">true</item>
    </style>

    <style name="CommonDialog" parent="Base.Theme.AppCompat.Dialog.FixedSize"></style>

    <style name="main_menu_animStyle">
        <!--windowEnterAnimation 进入的动画
        代码里面-->
        <item name="android:windowEnterAnimation">@anim/anim_comments_dialog_in_anim</item>
        <item name="android:windowExitAnimation">@anim/anim_comments_dialog_out_anim</item>
    </style>

</resources>
